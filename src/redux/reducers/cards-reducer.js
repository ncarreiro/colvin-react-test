export const initialState = {
  cards: []
};

function cardsReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}

export default cardsReducer;
